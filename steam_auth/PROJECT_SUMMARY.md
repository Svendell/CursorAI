# Steam Auth Manager - Project Summary

## 📱 Описание

**Steam Auth Manager** - это полнофункциональное приложение на Python для управления Steam аккаунтами с поддержкой двухфакторной аутентификации (Steam Guard 2FA).

Приложение разработано на **Kivy** для кроссплатформенной поддержки (Android, iOS, Windows, macOS, Linux).

## ✨ Основные возможности

### 1. Управление аккаунтами
- ✅ Просмотр списка всех Steam аккаунтов
- ✅ Добавление новых аккаунтов (вручную, через mafile, импорт)
- ✅ Редактирование данных аккаунта (имя, пароль, shared_secret)
- ✅ Удаление аккаунтов из приложения
- ✅ Локальное хранилище в SQLite БД

### 2. Steam Guard 2FA
- ✅ Генерация 5-значных кодов 2FA
- ✅ Автоматическое обновление кодов каждые 30 секунд
- ✅ Поддержка TOTP алгоритма (Time-based One-Time Password)
- ✅ Отображение 2FA кода на странице аккаунта

### 3. Mafile поддержка
- ✅ Создание mafiles (как в Steam Desktop Authenticator)
- ✅ Импорт существующих mafiles
- ✅ Экспорт аккаунтов в mafile формат
- ✅ Валидация mafiles
- ✅ Совместимость с SDA форматом

### 4. Подтверждения операций
- ✅ Просмотр списка операций для подтверждения
- ✅ Принятие операций (Accept/Confirm)
- ✅ Отклонение операций (Reject/Decline)
- ✅ Интеграция с Steam API

### 5. Безопасность
- ✅ Шифрование паролей (AES-256-CBC)
- ✅ Мастер пароль для защиты приложения
- ✅ PBKDF2 для производства ключей
- ✅ Безопасное хранилище (SecureStorage)

### 6. Конфигурация и логирование
- ✅ Файл конфигурации (config.ini)
- ✅ Логирование с ротацией файлов
- ✅ Настройка уровня логирования
- ✅ Автоматическое резервное копирование БД

## 📁 Структура проекта

```
steam_auth/
├── main.py                      # Главное приложение (точка входа)
├── config.ini                   # Файл конфигурации
├── requirements.txt             # Зависимости Python
├── buildozer.spec              # Конфиг для сборки Android
├── INSTALL.md                  # Инструкции по установке
├── README.md                   # Основная документация
├── example.py                  # Примеры использования
├── tests.py                    # Модульные тесты
├── accounts.db                 # База данных (создается автоматически)
├── mafiles/                    # Папка с mafiles
├── backups/                    # Резервные копии БД
├── logs/                       # Логи приложения
│
└── app/
    ├── __init__.py            # Package инициализация
    ├── database.py            # Работа с SQLite БД
    ├── steam_guard.py         # Логика Steam Guard и mafiles
    ├── steam_utils.py         # Утилиты для Steam API
    ├── encryption.py          # Шифрование паролей
    ├── config.py              # Менеджер конфигурации
    ├── logger.py              # Система логирования
    └── screens.py             # Все экраны приложения (Kivy UI)
```

## 🔧 Технологический стек

### Backend
- **Python 3.8+** - язык программирования
- **SQLite** - база данных
- **PyCryptodome** - криптография (AES, PBKDF2, HMAC)

### Frontend
- **Kivy 2.2.1** - кроссплатформенный UI фреймворк
- **Kivy Screen Manager** - навигация между экранами
- **Box/Grid Layouts** - расположение элементов

### Сборка
- **Buildozer** - сборка Android APK
- **Gradle** - сборка для Android

## 📱 Экраны приложения

### 1. HomeScreen - Главный экран
- Кнопка "Accounts" с числом аккаунтов
- Кнопки Settings и About
- Простой и интуитивный интерфейс

### 2. AccountsScreen - Список аккаунтов
- Список всех аккаунтов (по 4 на странице)
- Прокручиваемый список (70% высоты экрана)
- Кнопка "Add Account" для добавления
- Нажатие на аккаунт открывает его страницу

### 3. AccountScreen - Страница аккаунта
- Информация об аккаунте (имя, дата создания)
- **Live 2FA код** (обновляется каждые 30 секунд)
- Кнопка "Confirmations" - список операций
- Кнопка "Edit" - редактирование данных
- Кнопка "Delete" - удаление аккаунта

### 4. EditAccountScreen - Редактирование
- Поле для изменения имени аккаунта
- Поле для изменения пароля
- Поле для изменения shared_secret
- Кнопки Save и Cancel

### 5. ConfirmationsScreen - Подтверждения
- Список операций (Trade, Market, etc.)
- Кнопки Accept/Reject для каждой операции
- Кнопка Back для возврата

### 6. AddAccountScreen - Способ добавления
- 3 варианта добавления:
  - **Add Manually** - ввод данных вручную
  - **Create Mafile** - создание mafile
  - **Import Mafile** - импорт существующего mafile

### 7. ManualAddScreen - Ручное добавление
- Ввод имени аккаунта
- Ввод пароля
- Ввод shared_secret (base64)
- Кнопки Save и Cancel

### 8. CreateMafileScreen - Создание Mafile
- Те же поля что и ручное добавление
- После сохранения создается mafile в папке `mafiles/`
- Аккаунт добавляется в БД

### 9. ImportMafileScreen - Импорт Mafile
- Ввод пути к файлу mafile
- Ввод пароля для аккаунта
- После импорта аккаунт добавляется в БД

## 🗄️ База данных

### Таблица: accounts
```sql
CREATE TABLE accounts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_name TEXT UNIQUE NOT NULL,
    password TEXT NOT NULL,
    shared_secret TEXT NOT NULL,
    identity_secret TEXT,
    revocation_code TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

## 🔐 Безопасность

### Реализовано
✅ **Шифрование паролей** - AES-256-CBC
✅ **Мастер пароль** - защита приложения
✅ **PBKDF2** - производство ключей (100,000 итераций)
✅ **HMAC-SHA1** - для Steam Guard кодов
✅ **Валидация mafiles** - проверка целостности

### Рекомендации
- ⚠️ Используйте сильный мастер пароль
- ⚠️ Регулярно создавайте резервные копии
- ⚠️ Не делитесь shared_secret с кем-то еще
- ⚠️ Используйте только на доверенных устройствах

## 🚀 Быстрый старт

### На рабочем столе
```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить приложение
python main.py
```

### На Android
```bash
# Установить buildozer
pip install buildozer

# Собрать APK
buildozer android debug

# Установить на устройство
adb install bin/steamauth-1.0-debug.apk
```

## 📚 API / Методы

### Database API
```python
db = Database()
db.add_account(name, password, shared_secret)
db.get_account(account_id)
db.get_all_accounts()
db.update_account(id, name=None, password=None, shared_secret=None)
db.delete_account(account_id)
db.count_accounts()
```

### Steam Guard API
```python
guard = SteamGuardManager()
code = guard.get_steam_guard_code(shared_secret)
guard.create_mafile_from_dict(account_data)
guard.import_mafile(mafile_path)
confirmations = guard.get_confirmation_operations(...)
guard.confirm_operation(id, identity_secret, shared_secret, confirm=True)
```

### Configuration API
```python
config = get_config()
config.get('section', 'option', fallback)
config.get_int('section', 'option')
config.get_bool('section', 'option')
config.set('section', 'option', value)
```

## 🧪 Тестирование

Запустить тесты:
```bash
python tests.py
```

Тесты покрывают:
- ✅ Операции с БД
- ✅ Генерация TOTP кодов
- ✅ Валидация mafiles
- ✅ Шифрование паролей
- ✅ Конвертация SteamID

## 📦 Зависимости

```
kivy==2.2.1
pycryptodomex==3.18.0
requests==2.31.0
python-dateutil==2.8.2
Pillow==10.0.0
certifi==2023.7.22
```

## 🔄 Workflow приложения

```
HomeScreen
    ↓
Accounts (список) ← → Add Account (выбор способа)
    ↓              ↓
    ├─→ Manual Add (ввод данных)
    ├─→ Create Mafile (создание)
    └─→ Import Mafile (импорт)
    ↓
Account Detail
    ├─→ Edit (редактирование)
    ├─→ Delete (удаление)
    └─→ Confirmations (операции)
```

## 📄 Примеры конфигурации

### Включить шифрование паролей
```ini
[SECURITY]
encrypt_passwords = true
require_master_password = true
```

### Изменить размер окна
```ini
[UI]
window_width = 400
window_height = 900
```

### Включить debug логирование
```ini
[LOGGING]
log_level = 2
log_file = logs/steamauth.log
```

## 🎯 Планы развития

- [ ] Поддержка биометрической аутентификации
- [ ] Cloud sync (опциональный)
- [ ] Экспорт в CSV/JSON
- [ ] Backup и restore функции
- [ ] QR код для быстрого добавления
- [ ] Темный режим
- [ ] Мультиязычность (en, ru)
- [ ] Поддержка нескольких профилей

## 📝 Лицензия

MIT License

## 👥 Автор

Steam Auth Manager Team

## 🤝 Поддержка

Для вопросов и проблем:
1. Проверьте документацию в README.md
2. Посмотрите в INSTALL.md инструкции
3. Запустите тесты: `python tests.py`
4. Посмотрите логи: `logs/steamauth.log`

---

**Версия**: 1.0.0
**Дата создания**: 2026-01-15
**Статус**: Production Ready
